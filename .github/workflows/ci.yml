# CI workflow disabled
# The original CI workflow was intentionally removed/disabled per project owner request.
# To restore the CI, revert this file to the previous version or restore from VCS history.

# Empty trigger -> workflow will not run
on: []

# No jobs defined while CI is disabled
# If you want to re-enable, replace this file with the previous .github/workflows/ci.yml content
# or add a new workflow file under .github/workflows/ with the desired jobs.
